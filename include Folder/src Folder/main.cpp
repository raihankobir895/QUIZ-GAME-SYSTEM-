// main.cpp - intentionally empty in the GUI build.
//
// The program's entry point is now WinMain() in GuiApp.cpp, so this file is
// NOT part of the GUI build command. It is kept (not deleted) only so the
// project layout stays the same; the old console main() was removed because
// MainMenu::displayMenu() no longer exists.
